export enum Sorts {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum OutputFormats {
  EXPORT = 'export',
  LIST = 'list',
}

export enum ActivationStatus {
  ACTIVE = 'ACTIVE',
  DISABLE = 'DISABLE',
}

export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}
